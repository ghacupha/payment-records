version: '2'
services:
  paymentrecords-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/paymentRecords/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=paymentRecords
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
